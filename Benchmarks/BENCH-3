(DEFINE-FILE-INFO :PACKAGE "INTERLISP" :READTABLE "INTERLISP" :BASE 10)

(FILECREATED "21-May-2026 14:59:50" {GABRIEL}<Benchmarks>BENCH-3.;3 1362   

      :CHANGES-TO (FILES IO-BENCHMARKS GABRIEL-FILES GABRIEL-OTHER)
                  (FNS BENCH-3)
                  (VARS BENCH-3COMS)

      :PREVIOUS-DATE " 5-May-2026 16:13:19" {GABRIEL}<Benchmarks>Bench-3>BENCH-3.;9)


(PRETTYCOMPRINT BENCH-3COMS)

(RPAQQ BENCH-3COMS ((FILES (COMPILED)
                           GABRIEL-FILES)
                    (FILES (FROM VALUEOF *BENCHMARKS-TOOLS-DIR* COMPILED)
                           GABRIEL-PKG GABRIEL-TIMERS GABRIEL-OTHER)
                    (FILES (COMPILED)
                           IO-BENCHMARKS)
                    (FNS BENCH-3)
                    (XCL:FILE-ENVIRONMENTS BENCH-3)))

(FILESLOAD (COMPILED)
       GABRIEL-FILES)

(FILESLOAD (FROM VALUEOF *BENCHMARKS-TOOLS-DIR* COMPILED)
       GABRIEL-PKG GABRIEL-TIMERS GABRIEL-OTHER)

(FILESLOAD (COMPILED)
       IO-BENCHMARKS)
(DEFINEQ

(BENCH-3
  [LAMBDA NIL

    (* ;; "do a prophylactic GC")

    (RECLAIM)

    (* ;; "run the benchmarks")

    (GABRIEL::RUN-BENCHMARKS GABRIEL::*CONSY-BENCHMARKS* (MAKE-RESULTS-FILE-NAME "CONSY"])
)

(XCL:DEFINE-FILE-ENVIRONMENT BENCH-3 :PACKAGE "IL"
   :READTABLE "INTERLISP"
   :COMPILER :COMPILE-FILE)
(DECLARE%: DONTCOPY
  (FILEMAP (NIL (1008 1229 (BENCH-3 1018 . 1227)))))
STOP
